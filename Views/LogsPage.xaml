<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="OpenClawAdapter.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:OpenClawAdapter"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer>
        <StackPanel Padding="5,3,5,5" Spacing="5">
            <TextBlock Style="{StaticResource TitleText}" Text="日志与高级" />

            <Border Style="{StaticResource Card}">
                <StackPanel Spacing="5">
                    <TextBlock Style="{StaticResource SectionTitle}" Text="日志" />
                    <TextBlock>
                        <Run Text="日志文件：" />
                        <Run Text="{Binding LogFilePath}" />
                    </TextBlock>
                    <ToggleSwitch
                        Header="启用调试日志"
                        IsOn="{Binding DebugLoggingEnabled, Mode=TwoWay}" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel Spacing="5">
                    <TextBlock Style="{StaticResource SectionTitle}" Text="高级" />
                    <TextBlock>
                        <Run Text="配置热重载：" />
                        <Run Text="{Binding LastReloadText}" />
                    </TextBlock>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>


